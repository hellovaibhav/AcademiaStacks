name: 'Enforce Development to Main PRs'

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches:
      - main

jobs:
  enforce_branch_source:
    runs-on: ubuntu-latest
    steps:
      - name: 'Check that PR source is the development branch'
        if: github.head_ref != 'development'
        run: |
          echo "ERROR: Pull Requests to the 'main' branch must come from the 'development' branch."
          exit 1