# AcademiaStacks API Environment Variables
# Copy this file to .env and fill in your actual values

# ===========================================
# REQUIRED ENVIRONMENT VARIABLES
# ===========================================
# These variables are validated on startup and are required for the app to function

# MongoDB connection string
# For local development: mongodb://localhost:27017/academiastacks
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/academiastacks
MONGODB_URI=mongodb://localhost:27017/academiastacks

# JWT secret for token signing
# Generate a secure key: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Session secret for session management
# Generate a secure key: openssl rand -base64 32
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Gmail credentials for sending OTP verification emails
# Use App Password, not your regular Gmail password
# Enable 2FA and generate App Password: https://myaccount.google.com/apppasswords
MAILID=your-email@gmail.com
MAILPASS=your-app-password

# ===========================================
# OPTIONAL ENVIRONMENT VARIABLES
# ===========================================
# These variables have default values or are optional

# Server port (default: 8800)
PORT=8800

# Environment (development, production, test)
NODE_ENV=development

# JWT refresh token secret (defaults to JWT_SECRET if not provided)
JWT_REFRESH=your-jwt-refresh-secret

# Admin email for notifications 
ADMIN_EMAIL=admin@example.com

# Client URL for password reset links (defaults to http://localhost:3000)
CLIENT_URL=http://localhost:3000

# Google Drive integration (optional)
# Path to Google Drive service account key file
GOOGLE_DRIVE_KEY_FILE=./google-drive-key.json

# Google Drive folder ID for storing files
GOOGLE_DRIVE_FOLDER_ID=your-google-drive-folder-id

# ===========================================
# NOTES FOR CONTRIBUTORS
# ===========================================
# 1. Copy this file to .env in the api/ directory
# 2. Replace all placeholder values with your actual configuration
# 3. Never commit the .env file to version control
# 4. The 5 required variables are validated on startup
# 5. Generate strong, random secrets for JWT_SECRET and SESSION_SECRET
# 6. Use App Passwords for Gmail, not your regular password
# 7. Test your configuration with the health check endpoint: GET /
